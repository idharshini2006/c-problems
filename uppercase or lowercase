#include<stdio.h>
int main() {
    char ch;
    scanf("%c",&ch);
    if (ch>='A' && ch<='Z') {
        printf("Uppercase letter",ch);
    }
    else if (ch>='a' && ch<='z') {
        printf("Lowercase letter",ch);
    }
    else{
        printf("Not a letter",ch);
    }
    return 0;
}
