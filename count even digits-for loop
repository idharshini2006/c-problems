#include<stdio.h>
int main() {
    int n,lastDigit;
    int count=0;
    scanf("%d",&n);
    for (;n>0;n/=10) {
        lastDigit=n%10;
        if (lastDigit%2==0) {
            count++;
        }
    }
    printf("%d",count);
    return 0;
}
