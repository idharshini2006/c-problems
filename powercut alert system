#include<stdio.h>
int main() {
    int hours;
    scanf("%d",&hours);
    if (hours<=1) {
        printf("Short interuption");
    }
    else if (hours<=4) {
        printf("Medium interuption");
    }
    else {
        printf("Long interuption");
    }
    return 0;
}
